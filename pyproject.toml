[project]
name = "heatedtopics"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.59.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
