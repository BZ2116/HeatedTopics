# Toutiao 项目协作规范

## 项目目标

本项目围绕用户画像生成检索关键词，抓取、筛选并汇总今日头条热点内容。

## 目录约定

- `src/heated_topics_v3/`：核心业务代码；模块名使用小写蛇形命名。
- `tests/`：pytest 测试；测试文件命名为 `test_<module>.py`。
- `config/profiles/`：用户画像 JSON；文件名使用稳定的 `user_id`。
- `scripts/`：可直接执行的项目脚本，不放核心业务逻辑。
- `docs/superpowers/specs/`：已确认的设计规格。
- `docs/superpowers/plans/`：已确认规格对应的实施计划。
- `cache/`、`outputs/`、`state/`：运行时产物，不作为业务源码维护。

## 修改规则

- 行为变更先写测试并确认测试因目标行为缺失而失败，再修改实现。
- 修改提示词时同步检查解析、校验、缓存和降级逻辑，避免提示词与代码契约冲突。
- 不覆盖工作区中与当前任务无关或来源不明的未提交改动。
- 不读取、修改或输出 `.env`、Cookie、密钥和 token。
- 删除文件、修改 CI/CD、数据库迁移、安装全局依赖、发布、`git push`、`git rebase`、`git reset --hard` 前必须取得用户明确授权。

## 验证规则

- 关键词模块定向验证：`uv run pytest tests/test_llm_keywords.py -q`
- 完整测试：`uv run pytest -q`
- 仅在上述命令与当前环境不兼容时使用项目现有虚拟环境中的等价命令，并说明原因。

## 文档与清理

- 文档使用 UTF-8；中文说明优先，代码、命令和标识符使用英文。
- 临时调试文件不进入源码目录；任务完成后清理本任务新建的临时文件。
- 缓存和输出按运行需要生成，不手工提交，除非任务明确要求提供样例。
